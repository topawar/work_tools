# 需求计划明细日期修改功能说明

## 功能概述

本功能用于批量修改需求计划明细的日期信息，支持修改需用日期、开始日期、结束日期。

## 涉及表

1. **tprnd02** - 需求计划表
2. **tprly02** - 采购包表
3. **tprfa03** - 采购方案表
4. **tprxj05** - 询价单表

## 字段说明

- **plan_line_no**: 明细行号（必填）
- **required_date**: 需用日期（YYYYMMDD 格式，非必填）
- **start_date**: 开始日期（YYYYMMDD 格式，非必填）
- **end_date**: 结束日期（YYYYMMDD 格式，非必填）
- **orig\_\***: 原日期字段，用于生成回退语句（可选）

## 使用方式

### 单条修改

1. 填写动态编号和操作备注
2. 填写明细行号
3. 至少填写一个日期字段（需用日期/开始日期/结束日期）
4. 可选填写原日期字段用于生成回退语句
5. 点击"生成 SQL"

### 批量导入

1. 下载 Excel 模板
2. 按照模板格式填写数据
   - 明细行号为必填项
   - 日期字段为非必填项，仅对填写的字段生成 SQL
   - 原日期字段用于生成回退语句
3. 上传 Excel 文件
4. 点击"生成 SQL"

## SQL 生成规则

### 执行语句

- 按表分组生成：需求计划 → 采购包 → 采购方案 → 询价单
- 仅对填写的日期字段生成更新语句
- 自动添加 ops_remark 操作备注

### 回退语句

- 仅当填写了原日期字段时才生成对应的回退 SQL
- 回退语句按相同顺序分组

## SQL 合并策略

- 支持通过系统配置开启/关闭 SQL 合并
- 启用后，相同条件的语句会合并为 IN 查询

## 示例

### 示例 1：仅修改需用日期

```
明细行号: ZH25-XQJH-25-00028-000001
需用日期: 20250531
原需用日期: 20250430
```

生成 SQL：

```sql
-- 需求计划
update tprnd02 set required_date='20250531',ops_remark='...' where plan_line_no='ZH25-XQJH-25-00028-000001';

-- 采购包
update tprly02 set OPS_REMARK='...',REQUIRED_DATE='20250531' where plan_line_no='ZH25-XQJH-25-00028-000001' and ALIVE_FLAG='1';

-- 采购方案
update tprfa03 set OPS_REMARK='...',REQUIRED_DATE='20250531' where plan_line_no='ZH25-XQJH-25-00028-000001' and ALIVE_FLAG='1';

-- 询价单
update tprxj05 set OPS_REMARK='...',REQUIRED_DATE='20250531' where plan_line_no='ZH25-XQJH-25-00028-000001' and ALIVE_FLAG='1';
```

### 示例 2：修改多个日期

```
明细行号: ZJXC-XQJH-25-00758-000001
需用日期: 20250902
开始日期: 20250902
```

生成 SQL 会包含所有填写的日期字段。

## 注意事项

1. 明细行号为必填项
2. 至少需要填写一个日期字段
3. 日期格式必须为 YYYYMMDD（8 位数字）
4. Excel 导入支持列名忽略大小写
5. SQL 按需求计划-询价单顺序归类分组生成
6. 校验失败的记录会生成单独的失败文件

## 数据库信息

- IP: 192.168.11.76
- 库名: cnnc_ph
