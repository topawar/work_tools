# 合同单位修改 - 原单位名称校验最终解决方案

## 🎯 问题解决

### 原问题
用户选择了 `中核（上海）供应链管理有限公司西北分公司-99280563919161110148`，但校验时只用名称查询，导致无法精确匹配。

### 解决方案
实现了智能解析和分层校验，支持精确匹配和友好提示。

## ✅ 核心功能

### 1. 智能输入解析
```python
def parse_unit_name_with_code(input_value):
    # 自动识别两种格式：
    # 1. 纯名称：中核（上海）供应链管理有限公司
    # 2. 名称-编码：中核（上海）供应链管理有限公司-99280563919161110148
```

### 2. 分层校验策略
- **有编码**：精确匹配（名称+编码）
- **无编码，唯一记录**：通过校验
- **无编码，多条记录**：提示选择具体编码
- **名称不存在**：校验失败
- **编码错误**：校验失败

### 3. 友好错误提示
- `原起草单位名称"公司名"存在多条记录，请选择具体编码：编码1、编码2`
- `原起草单位名称"公司名"（编码：错误编码）在数据库中不存在`

## 🔍 实际效果

### 用户选择带编码的选项
```
输入：中核（上海）供应链管理有限公司西北分公司-99280563919161110148
解析：名称="中核（上海）供应链管理有限公司西北分公司", 编码="99280563919161110148"
校验：WHERE company_name='...' AND company_code='99280563919161110148'
结果：✓ 校验通过（找到1条精确匹配的记录）
```

### 用户只输入名称（多条记录）
```
输入：中核（上海）供应链管理有限公司西北分公司
解析：名称="中核（上海）供应链管理有限公司西北分公司", 编码=None
校验：WHERE company_name='中核（上海）供应链管理有限公司西北分公司'
结果：⚠ 提示"存在多条记录，请选择具体编码：99280563919161110148、1912178770857907780"
```

### 用户只输入名称（唯一记录）
```
输入：中核新疆供应链有限公司
解析：名称="中核新疆供应链有限公司", 编码=None
校验：WHERE company_name='中核新疆供应链有限公司'
结果：✓ 校验通过（找到1条唯一记录）
```

## 🎨 用户体验

### 自动完成支持
- 搜索API返回 `公司名称-编码` 格式
- 用户看到并可选择带编码的选项
- 避免手动输入错误

### 错误处理
- 清晰的错误信息，指出具体问题
- 建设性的建议，列出可用编码
- 表单数据保持，便于修改

### 输入灵活性
- 支持自动完成选择（推荐）
- 支持手动输入纯名称
- 支持手动输入名称-编码格式

## 📋 测试验证

### 数据库现状
```sql
-- 同名多编码的情况
SELECT company_name, company_code FROM org_detail 
WHERE company_name = '中核（上海）供应链管理有限公司西北分公司';

结果：
中核（上海）供应链管理有限公司西北分公司|99280563919161110148
中核（上海）供应链管理有限公司西北分公司|1912178770857907780
```

### 校验测试
- ✅ 精确匹配（名称+编码）：通过
- ✅ 唯一名称：通过  
- ⚠️ 多条记录：提示选择编码
- ❌ 不存在名称：失败
- ❌ 错误编码：失败

## 🚀 技术优势

1. **精确性**：解决同名组织机构的歧义
2. **智能性**：自动识别输入格式
3. **友好性**：提供清晰的错误提示
4. **兼容性**：支持原有输入方式
5. **性能**：高效的数据库查询
6. **扩展性**：易于添加新规则

## 💡 使用建议

### 对用户
1. **优先使用自动完成功能**
2. **选择带编码的选项确保精确匹配**
3. **根据错误提示选择正确编码**

### 对管理员
1. **保持组织机构数据更新**
2. **监控校验失败情况**
3. **定期清理重复数据**

## 🔧 实现细节

### 代码位置
- `work_tools/views/contract_unit.py`
- 新增 `parse_unit_name_with_code()` 函数
- 增强 `validate_original_unit_names()` 函数

### 集成点
- 单条记录模式：表单校验
- Excel批量导入：逐行校验

### 依赖
- Django ORM
- `OrgDetail` 模型
- 现有的自动完成API

## 🎉 最终效果

现在用户选择 `中核（上海）供应链管理有限公司西北分公司-99280563919161110148` 时：

1. **解析正确**：自动分离名称和编码
2. **校验精确**：使用名称+编码进行精确匹配
3. **结果准确**：找到对应记录，校验通过
4. **体验良好**：无错误提示，可以正常生成SQL

问题完美解决！✨