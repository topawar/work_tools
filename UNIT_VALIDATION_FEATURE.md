# 合同单位修改 - 原单位名称校验功能

## 功能描述
在"合同起草、签约单位修改"页面中，当用户填写了"原起草单位名称"或"原签约主体名称"时，系统会在生成SQL前校验这些名称是否在组织机构数据库中存在。如果不存在，将阻止SQL生成并显示错误提示。

## 校验规则

### 1. 校验时机
- 点击"生成SQL"按钮时触发校验
- 在其他基础校验（必填项、标识字段）通过后执行

### 2. 校验对象
- **原起草单位名称** (`old_drafting_name`)
- **原签约主体名称** (`old_party_name`)

### 3. 校验逻辑
- 使用**精确匹配**：`company_name = '输入的名称'`
- 不支持部分匹配或模糊匹配
- 只有在`org_detail`表中存在完全相同的记录才能通过校验

## 使用场景

### 单条记录模式
```
用户输入：
- 原起草单位名称: "中核（上海）供应链管理有限公司"
- 原签约主体名称: "不存在的公司名称"

校验结果：
✗ 原签约主体名称"不存在的公司名称"在数据库中不存在
```

### Excel批量导入模式
```
Excel文件内容：
第2行: 原起草单位名称 = "存在的公司A"     ✓
第3行: 原起草单位名称 = "不存在的公司B"   ✗
第4行: 原签约主体名称 = "不存在的公司C"   ✗

校验结果：
第3行: 原起草单位名称"不存在的公司B"在数据库中不存在
第4行: 原签约主体名称"不存在的公司C"在数据库中不存在
```

## 错误提示格式

### 单条记录模式
- `原起草单位名称"{名称}"在数据库中不存在`
- `原签约主体名称"{名称}"在数据库中不存在`

### Excel批量导入模式
- `第{行号}行: 原起草单位名称"{名称}"在数据库中不存在`
- `第{行号}行: 原签约主体名称"{名称}"在数据库中不存在`

## 技术实现

### 1. 校验函数
```python
def validate_original_unit_names(old_drafting_name=None, old_party_name=None):
    """校验原单位名称是否在数据库中存在"""
    errors = []
    
    if old_drafting_name:
        exists = OrgDetail.objects.filter(company_name=old_drafting_name).exists()
        if not exists:
            errors.append(f'原起草单位名称"{old_drafting_name}"在数据库中不存在')
    
    if old_party_name:
        exists = OrgDetail.objects.filter(company_name=old_party_name).exists()
        if not exists:
            errors.append(f'原签约主体名称"{old_party_name}"在数据库中不存在')
    
    return errors
```

### 2. 集成位置
- **单条记录模式**：在基础校验之后，SQL生成之前
- **Excel批量导入**：在现有数据校验之后，名称转换之前

## 用户体验优化

### 1. 自动完成功能
- 页面已有自动完成功能，用户输入时会显示匹配的单位名称
- 建议用户使用自动完成选择，避免手动输入错误

### 2. 错误处理
- 校验失败时，表单保持用户输入的数据
- 错误信息清晰指出具体哪个字段的哪个值不存在
- 支持同时显示多个错误信息

### 3. 数据源参考
当前数据库中包含的中核供应链相关单位：
- 中核（上海）供应链管理有限公司
- 中核新疆供应链有限公司  
- 中核（上海）供应链管理有限公司北京分公司
- 中核（上海）供应链管理有限公司西北分公司

## 注意事项

1. **精确匹配要求**：必须输入完整的单位名称，不能使用简称或部分名称
2. **大小写敏感**：输入的名称必须与数据库中的记录完全一致
3. **空值处理**：如果原单位名称为空，不进行校验（允许为空）
4. **性能考虑**：校验使用数据库索引查询，性能良好

## 测试建议

### 正常情况测试
- 输入存在的完整单位名称 → 应该通过校验
- 原单位名称为空 → 应该跳过校验

### 异常情况测试  
- 输入不存在的单位名称 → 应该显示错误提示
- 输入部分单位名称 → 应该显示错误提示
- 输入错误的大小写 → 应该显示错误提示

### Excel批量测试
- 混合存在和不存在的单位名称 → 应该准确指出每行的错误