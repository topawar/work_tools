# 合同单位修改 - 原单位名称校验功能实现总结

## 🎯 需求
在"合同起草、签约单位修改"页面，当用户填写原起草单位名称或原签约主体名称时，点击生成SQL前需要校验这些名称是否在数据库中存在。如果不存在，不允许生成SQL并提示错误。

## ✅ 实现完成

### 1. 核心校验函数
```python
def validate_original_unit_names(old_drafting_name=None, old_party_name=None):
    """校验原单位名称是否在数据库中存在"""
    errors = []
    
    if old_drafting_name:
        exists = OrgDetail.objects.filter(company_name=old_drafting_name).exists()
        if not exists:
            errors.append(f'原起草单位名称"{old_drafting_name}"在数据库中不存在')
    
    if old_party_name:
        exists = OrgDetail.objects.filter(company_name=old_party_name).exists()
        if not exists:
            errors.append(f'原签约主体名称"{old_party_name}"在数据库中不存在')
    
    return errors
```

### 2. 单条记录模式集成
- 在基础校验（必填项、标识字段）之后执行
- 校验失败时显示错误信息，阻止SQL生成
- 保持用户输入的表单数据

### 3. Excel批量导入模式集成
- 在现有数据校验之后执行
- 逐行校验，显示具体行号和错误信息
- 支持同时显示多个错误

### 4. 错误信息格式
- **单条记录**：`原起草单位名称"{名称}"在数据库中不存在`
- **批量导入**：`第{行号}行: 原起草单位名称"{名称}"在数据库中不存在`

## 🔍 校验规则

### 校验对象
- `old_drafting_name` - 原起草单位名称
- `old_party_name` - 原签约主体名称

### 校验逻辑
- **精确匹配**：使用 `company_name = '输入值'` 查询
- **非空校验**：只对非空值进行校验
- **数据源**：查询 `org_detail` 表中的 `company_name` 字段

### 校验时机
- 点击"生成SQL"按钮时
- 在其他基础校验通过后执行
- 在SQL生成前执行

## 📋 测试验证

### 测试数据准备
```sql
-- 存在的单位名称
SELECT company_name FROM org_detail WHERE company_name = '中核（上海）供应链管理有限公司';
-- 结果：1条记录

-- 不存在的单位名称  
SELECT company_name FROM org_detail WHERE company_name = '不存在的公司名称测试';
-- 结果：0条记录
```

### 测试场景
1. ✅ **正常情况**：输入存在的完整单位名称 → 校验通过
2. ✅ **空值情况**：原单位名称为空 → 跳过校验
3. ✅ **不存在情况**：输入不存在的单位名称 → 显示错误
4. ✅ **部分匹配情况**：输入部分名称 → 显示错误
5. ✅ **批量导入**：混合存在/不存在的名称 → 准确报错

## 🎨 用户体验

### 错误显示
- 使用Bootstrap的 `alert alert-danger` 样式
- 错误信息清晰明确，指出具体字段和值
- 支持同时显示多个错误信息

### 表单保持
- 校验失败时保持用户输入的数据
- 用户可以修改错误的字段后重新提交

### 自动完成支持
- 页面已有自动完成功能
- 用户可以通过自动完成选择正确的单位名称
- 避免手动输入错误

## 🔧 技术细节

### 数据库查询
- 使用Django ORM的 `exists()` 方法，性能优良
- 精确匹配查询，避免误判
- 利用数据库索引提升查询速度

### 代码集成
- 在 `work_tools/views/contract_unit.py` 中实现
- 添加了 `OrgDetail` 模型导入
- 集成到现有的表单处理流程中

### 错误处理
- 使用Django的 `form.add_error(None, error)` 添加非字段错误
- 错误信息支持中文显示
- 与现有错误处理机制兼容

## 📖 使用说明

### 单条记录模式
1. 填写表单字段，包括原单位名称
2. 点击"生成SQL"按钮
3. 如果原单位名称不存在，会显示错误提示
4. 修正错误后重新提交

### Excel批量导入模式
1. 准备Excel文件，包含原单位名称列
2. 上传文件并点击"生成SQL"
3. 系统会校验每行的原单位名称
4. 如有错误，会显示具体行号和错误信息

### 最佳实践
1. **使用自动完成**：输入时选择提示的单位名称
2. **完整名称**：输入完整的单位名称，不要使用简称
3. **数据准确性**：确保组织机构数据库是最新的
4. **错误处理**：根据错误提示修正输入内容

## 🚀 功能优势

1. **数据完整性**：确保原单位名称的准确性
2. **用户友好**：清晰的错误提示和自动完成支持
3. **性能优良**：高效的数据库查询
4. **兼容性好**：与现有功能无缝集成
5. **可维护性**：代码结构清晰，易于维护

## 📝 后续建议

1. **数据维护**：定期更新组织机构数据库
2. **用户培训**：指导用户使用自动完成功能
3. **监控优化**：监控校验失败的情况，优化数据质量
4. **功能扩展**：可考虑添加模糊匹配建议功能