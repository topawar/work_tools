# 配置管理功能增强实施任务

## 任务列表

- [x] 1. 创建通用分页和搜索辅助工具


  - 创建 `work_tools/utils/pagination_helper.py` 文件
  - 实现 `paginate_queryset` 函数用于通用分页
  - 实现 `filter_by_search` 函数用于通用搜索过滤
  - _Requirements: 1.1, 2.1, 5.1, 6.1_





- [x] 2. 实现下拉框配置管理的搜索功能

  - [ ] 2.1 修改 `dropdown_config_view` 函数支持搜索参数
    - 添加 `search` 查询参数处理


    - 实现分组名称和编码的模糊搜索
    - 实现选项标签和编码的模糊搜索
    - _Requirements: 1.1, 1.2, 1.3_
  
  - [ ] 2.2 更新 `dropdown_config.html` 模板添加搜索框
    - 在分组列表上方添加搜索输入框
    - 添加清除搜索按钮
    - 实现搜索防抖 JavaScript 逻辑（500ms）
    - 显示搜索无结果提示
    - _Requirements: 1.1, 1.4, 1.5_

- [ ] 3. 实现下拉框配置管理的分页功能
  - [ ] 3.1 修改 `dropdown_config_view` 函数支持分页
    - 添加 `page` 查询参数处理
    - 使用 Django Paginator 对分组列表分页（每页10条）
    - 使用 Django Paginator 对选项列表分页（每页10条）
    - 处理分页越界情况
    - _Requirements: 2.1, 2.2, 2.4_
  
  - [ ] 3.2 更新 `dropdown_config.html` 模板添加分页控件
    - 在分组列表下方添加分页导航
    - 在选项列表下方添加分页导航
    - 显示当前页码和总页数
    - 添加上一页/下一页按钮
    - 分页链接保持搜索状态
    - _Requirements: 2.2, 2.3, 2.4, 2.5_

- [ ] 4. 实现下拉框配置管理的删除功能
  - [ ] 4.1 添加 `dropdown_group_delete` 视图函数
    - 实现删除分组及其所有选项的逻辑
    - 添加删除前的验证
    - 清除相关缓存
    - 记录删除日志
    - _Requirements: 3.2, 3.5_
  
  - [ ] 4.2 更新 `dropdown_config.html` 模板添加删除按钮
    - 在分组操作区添加删除按钮
    - 实现删除确认对话框
    - 显示删除成功/失败提示
    - _Requirements: 3.1, 3.5_
  
  - [ ] 4.3 更新 URL 路由配置
    - 在 `work_tools/urls.py` 添加删除分组的路由
    - 更新 `__all__` 导出列表
    - _Requirements: 3.1, 3.2_

- [ ] 5. 实现下拉框配置管理的状态管理功能
  - [ ] 5.1 确认现有状态切换功能正常
    - 测试分组启用/禁用功能
    - 测试选项启用/禁用功能
    - _Requirements: 4.1, 4.3_
  
  - [ ] 5.2 优化状态切换的用户反馈
    - 确保状态切换后显示成功提示
    - 确保禁用状态在UI上有明显标识
    - _Requirements: 4.2, 4.4, 4.5_

- [ ] 6. 实现可配置表管理的字段搜索功能
  - [ ] 6.1 修改 `configurable_config_view` 函数支持字段搜索
    - 添加 `field_search` 查询参数处理
    - 实现修改字段的搜索过滤
    - 实现查询字段的搜索过滤
    - _Requirements: 6.1, 6.2_
  
  - [ ] 6.2 更新 `configurable_config.html` 模板添加字段搜索框
    - 在修改字段标签页添加搜索框
    - 在查询字段标签页添加搜索框
    - 实现搜索防抖逻辑
    - 显示搜索无结果提示
    - _Requirements: 6.1, 6.3, 6.4_

- [ ] 7. 实现可配置表管理的字段分页功能
  - [ ] 7.1 修改 `configurable_config_view` 函数支持字段分页
    - 添加 `update_page` 查询参数处理修改字段分页
    - 添加 `query_page` 查询参数处理查询字段分页
    - 使用 Django Paginator 分页（每页10条）
    - 处理分页越界情况
    - _Requirements: 5.1, 5.2, 5.3_
  
  - [ ] 7.2 更新 `configurable_config.html` 模板添加字段分页控件
    - 在修改字段表格下方添加分页导航
    - 在查询字段表格下方添加分页导航
    - 显示当前页码和总页数
    - 分页链接保持表选择和搜索状态
    - _Requirements: 5.3, 5.4, 5.5, 6.5_

- [ ] 8. UI 统一化和样式优化
  - [ ] 8.1 创建统一的分页组件样式
    - 定义分页组件的 CSS 样式
    - 确保所有分页控件使用统一样式
    - _Requirements: 7.1, 7.4_
  
  - [ ] 8.2 创建统一的搜索框样式
    - 定义搜索框的 CSS 样式
    - 确保所有搜索框使用统一样式
    - _Requirements: 7.2_
  
  - [ ] 8.3 优化操作按钮布局
    - 确保删除、启用/禁用按钮布局一致
    - 优化按钮间距和对齐
    - _Requirements: 7.3_
  
  - [ ] 8.4 优化数据表格样式
    - 确保表格样式统一
    - 优化表格响应式布局
    - _Requirements: 7.4_
  
  - [ ] 8.5 统一提示信息样式
    - 确保成功/错误提示使用统一样式
    - 优化提示信息的位置和动画
    - _Requirements: 7.5_

- [ ] 9. 测试和验证
  - [ ] 9.1 测试下拉框配置管理功能
    - 测试搜索功能（分组名称、编码、选项）
    - 测试分页功能（翻页、越界处理）
    - 测试删除功能（空分组、包含选项的分组）
    - 测试搜索+分页组合
    - _Requirements: 1.1-1.5, 2.1-2.5, 3.1-3.5_
  
  - [ ] 9.2 测试可配置表管理功能
    - 测试字段搜索功能
    - 测试字段分页功能
    - 测试搜索+分页组合
    - 测试标签页切换时的状态保持
    - _Requirements: 5.1-5.5, 6.1-6.5_
  
  - [ ] 9.3 测试UI一致性
    - 验证所有分页控件样式一致
    - 验证所有搜索框样式一致
    - 验证所有按钮样式一致
    - 验证响应式布局正常
    - _Requirements: 7.1-7.5_

- [ ] 10. 文档和代码清理
  - 更新代码注释
  - 确保日志记录完整
  - 清理调试代码
  - 更新相关文档
