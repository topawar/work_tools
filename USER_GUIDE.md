# Work Tools 便携式应用程序用户指南

## 📋 概述

Work Tools 是一个便携式的Django工作工具应用程序，可以在任何Windows PC上运行，无需安装Python环境。该应用程序包含了完整的数据管理、配置管理和静态资源服务功能。

## 🚀 快速开始

### 系统要求

- **操作系统**: Windows 10/11 或 Windows Server 2019/2022
- **内存**: 至少 512MB 可用内存
- **磁盘空间**: 至少 200MB 可用空间
- **网络**: 可选（用于某些功能）

### 首次运行

1. **下载应用程序**
   - 从发布页面下载 `work_tools_portable.zip`
   - 解压到任意目录（建议：`C:\WorkTools\`）

2. **启动应用程序**
   - 双击 `work_tools.exe` 启动应用程序
   - 首次启动会自动：
     - 复制数据库文件
     - 初始化配置
     - 创建必要目录
     - 打开默认浏览器

3. **访问Web界面**
   - 应用程序会自动在默认浏览器中打开
   - 默认地址：`http://127.0.0.1:8000`
   - 如果端口被占用，会自动选择其他可用端口

## 📁 目录结构

```
work_tools/
├── work_tools.exe          # 主程序文件
├── db.sqlite3             # SQLite数据库文件
├── app_config.json        # 应用程序配置文件
├── static/                # 静态文件目录
│   ├── css/              # 样式文件
│   ├── js/               # JavaScript文件
│   └── images/           # 图片文件
├── templates/             # HTML模板文件
├── logs/                  # 日志文件目录
│   ├── requests.log      # 请求日志
│   ├── errors.log        # 错误日志
│   └── launcher.log      # 启动器日志
├── temp_files/           # 临时文件目录
└── temp_uploads/         # 上传文件临时目录
```

## ⚙️ 配置管理

### 配置文件位置

- **JSON配置**: `app_config.json`
- **数据库配置**: 存储在 `db.sqlite3` 中的 `app_config` 表

### 主要配置项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `app_name` | "Work Tools" | 应用程序名称 |
| `port` | 8000 | Web服务器端口 |
| `debug` | false | 调试模式（打包版本默认关闭） |
| `auto_open_browser` | true | 启动时自动打开浏览器 |
| `database_path` | "db.sqlite3" | 数据库文件路径 |
| `static_root` | "static" | 静态文件根目录 |
| `templates_root` | "templates" | 模板文件根目录 |

### 修改配置

1. **通过Web界面**（推荐）
   - 访问管理页面
   - 修改相应设置
   - 配置会自动保存

2. **手动编辑配置文件**
   ```json
   {
     "app_name": "我的工作工具",
     "port": 8080,
     "debug": false,
     "auto_open_browser": true
   }
   ```

## 🔧 使用功能

### 数据管理

1. **数据导入**
   - 支持Excel文件导入
   - 自动数据验证
   - 错误报告生成

2. **数据查询**
   - 灵活的查询条件
   - 结果导出功能
   - 数据统计分析

3. **数据备份**
   - 自动数据库备份
   - 手动备份功能
   - 备份文件管理

### 文件管理

1. **文件上传**
   - 拖拽上传支持
   - 批量文件处理
   - 文件类型验证

2. **文件下载**
   - 处理结果下载
   - 报告文件生成
   - 历史文件访问

## 🚚 便携性使用

### 复制到新电脑

1. **完整复制**
   - 复制整个应用程序目录
   - 包含所有数据和配置
   - 在新电脑上直接运行

2. **注意事项**
   - 确保目标电脑满足系统要求
   - 防火墙可能需要允许程序运行
   - 杀毒软件可能需要添加信任

### 多用户使用

1. **共享使用**
   - 多个用户可以同时访问Web界面
   - 数据会自动同步
   - 避免同时进行大量数据操作

2. **独立使用**
   - 每个用户可以有独立的应用程序副本
   - 数据完全隔离
   - 配置独立管理

## 🔍 故障排除

### 常见问题

#### 1. 应用程序无法启动

**症状**: 双击exe文件没有反应或立即关闭

**解决方案**:
- 检查是否有杀毒软件阻止
- 确认系统满足最低要求
- 查看 `logs/launcher.log` 文件中的错误信息
- 尝试以管理员身份运行

#### 2. 浏览器无法访问

**症状**: 浏览器显示"无法连接"或"页面不存在"

**解决方案**:
- 检查应用程序是否正在运行
- 确认端口没有被其他程序占用
- 尝试手动访问 `http://127.0.0.1:8000`
- 检查防火墙设置

#### 3. 数据库错误

**症状**: 页面显示数据库相关错误

**解决方案**:
- 检查 `db.sqlite3` 文件是否存在
- 确认文件没有被其他程序锁定
- 查看 `logs/errors.log` 中的详细错误
- 尝试从备份恢复数据库

#### 4. 静态文件无法加载

**症状**: 页面样式混乱或图片无法显示

**解决方案**:
- 检查 `static/` 目录是否完整
- 确认文件权限正确
- 重新启动应用程序
- 清除浏览器缓存

### 日志文件说明

| 日志文件 | 内容 | 用途 |
|----------|------|------|
| `launcher.log` | 启动器日志 | 诊断启动问题 |
| `requests.log` | HTTP请求日志 | 跟踪用户操作 |
| `errors.log` | 错误日志 | 诊断运行时错误 |
| `sql.log` | 数据库操作日志 | 诊断数据库问题 |

### 性能优化

#### 1. 内存使用优化

- 定期重启应用程序（建议每24小时）
- 避免同时处理大量数据
- 清理临时文件目录

#### 2. 数据库性能优化

- 定期清理历史数据
- 避免并发的大量数据操作
- 使用合适的查询条件

#### 3. 网络性能优化

- 使用本地网络访问
- 避免通过远程桌面使用
- 关闭不必要的网络服务

## 🔒 安全注意事项

### 数据安全

1. **定期备份**
   - 重要数据定期备份
   - 备份文件存储在安全位置
   - 测试备份文件的完整性

2. **访问控制**
   - 应用程序默认只允许本地访问
   - 不要在不安全的网络环境中使用
   - 注意保护包含敏感数据的文件

3. **文件权限**
   - 确保应用程序目录有适当的权限
   - 避免在共享目录中运行
   - 定期检查文件完整性

### 网络安全

1. **防火墙设置**
   - 应用程序只需要本地网络访问
   - 不要开放到互联网的访问
   - 监控异常的网络连接

2. **端口管理**
   - 使用默认端口范围（8000-8100）
   - 避免使用已知的服务端口
   - 定期检查端口使用情况

## 📞 技术支持

### 获取帮助

1. **查看日志文件**
   - 首先检查相关的日志文件
   - 记录错误信息和操作步骤
   - 准备系统环境信息

2. **性能监控**
   - 查看 `logs/performance_metrics.json`
   - 检查内存和CPU使用情况
   - 记录性能异常的时间点

3. **联系支持**
   - 提供详细的错误描述
   - 包含相关的日志文件
   - 说明操作系统和硬件信息

### 版本更新

1. **检查更新**
   - 定期检查是否有新版本
   - 查看更新日志了解新功能
   - 备份当前数据后再更新

2. **更新步骤**
   - 停止当前应用程序
   - 备份数据和配置文件
   - 下载新版本并解压
   - 复制数据和配置到新版本
   - 启动新版本并验证功能

## 📚 附录

### A. 配置文件模板

```json
{
  "app_name": "Work Tools",
  "version": "1.0.0",
  "port": 8000,
  "debug": false,
  "auto_open_browser": true,
  "database_path": "db.sqlite3",
  "static_root": "static",
  "templates_root": "templates",
  "logs_directory": "logs",
  "temp_directory": "temp_files",
  "secret_key": "your-secret-key-here",
  "allowed_hosts": ["localhost", "127.0.0.1"]
}
```

### B. 常用命令行参数

虽然这是一个图形界面应用程序，但也支持一些命令行参数：

```cmd
work_tools.exe --help          # 显示帮助信息
work_tools.exe --version       # 显示版本信息
work_tools.exe --port 8080     # 指定端口启动
work_tools.exe --no-browser    # 启动时不打开浏览器
```

### C. 环境变量

可以通过环境变量覆盖某些配置：

| 环境变量 | 说明 | 示例 |
|----------|------|------|
| `WORK_TOOLS_PORT` | 服务器端口 | `8080` |
| `WORK_TOOLS_DEBUG` | 调试模式 | `true` |
| `WORK_TOOLS_DB_PATH` | 数据库路径 | `data/app.db` |

---

**版本**: 1.0.0  
**更新日期**: 2024-12-15  
**文档语言**: 中文