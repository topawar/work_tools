<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>可配置表管理</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      body {
        background-color: #f8f9fa;
        padding: 15px;
      }
      .container-fluid {
        max-width: 1600px;
      }
      .config-layout {
        display: flex;
        gap: 20px;
        margin-top: 20px;
      }
      .table-list {
        flex: 0 0 280px;
        background: white;
        border-radius: 8px;
        padding: 15px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        max-height: calc(100vh - 150px);
        overflow-y: auto;
      }
      .table-item {
        padding: 12px 15px;
        margin-bottom: 8px;
        border-radius: 6px;
        cursor: pointer;
        transition: all 0.2s;
        border: 1px solid #e9ecef;
      }
      .table-item:hover {
        background: #f8f9fa;
        border-color: #0d6efd;
      }
      .table-item.active {
        background: #0d6efd;
        color: white;
        border-color: #0d6efd;
      }
      .table-display-name {
        font-weight: 500;
        margin-bottom: 4px;
      }
      .table-code {
        font-size: 12px;
        opacity: 0.8;
      }
      .table-item.active .table-code {
        opacity: 0.9;
      }
      .config-panel {
        flex: 1;
        background: white;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }
      .panel-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        padding-bottom: 15px;
        border-bottom: 2px solid #e9ecef;
      }
      .panel-title {
        font-size: 18px;
        font-weight: 600;
        color: #333;
      }
      .btn-group-small {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
      }
      .nav-tabs {
        border-bottom: 2px solid #dee2e6;
        margin-bottom: 20px;
      }
      .nav-tabs .nav-link {
        border: none;
        border-bottom: 2px solid transparent;
        color: #6c757d;
        padding: 10px 20px;
      }
      .nav-tabs .nav-link.active {
        color: #0d6efd;
        border-bottom-color: #0d6efd;
        background: none;
      }
      .badge-active {
        background: #28a745;
      }
      .badge-inactive {
        background: #dc3545;
      }
      .modal-body label {
        font-weight: 500;
        margin-top: 10px;
      }
      .save-notice {
        position: fixed;
        right: 16px;
        top: 16px;
        z-index: 9999;
        color: #fff;
        padding: 10px 14px;
        border-radius: 6px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
        transition: opacity 0.3s ease;
      }
      .save-success {
        background: #28a745;
      }
      .save-error {
        background: #dc3545;
      }
      .empty-state {
        text-align: center;
        padding: 60px 20px;
        color: #6c757d;
      }
      .empty-state div:first-child {
        font-size: 48px;
        margin-bottom: 15px;
      }
      .action-buttons {
        display: flex;
        flex-direction: row;
        gap: 6px;
        align-items: center;
        flex-wrap: nowrap;
      }
      .action-buttons form {
        margin: 0;
        display: inline;
      }
      .action-buttons .btn {
        white-space: nowrap;
        padding: 5px 10px;
        font-size: 13px;
      }
    </style>
  </head>
  <body>
    {% include 'sidebar.html' %}

    <div class="container-fluid">
      <h2 style="margin-bottom: 10px">可配置表管理</h2>
      <p style="color: #6c757d; font-size: 14px">配置通用数据修改页面的表和字段信息</p>

      {% if message %}
      <div id="saveSuccess" class="save-notice save-success">{{ message }}</div>
      {% endif %} {% if error %}
      <div id="saveError" class="save-notice save-error">{{ error }}</div>
      {% endif %}

      <div class="config-layout">
        <!-- 左侧:表配置列表 -->
        <div class="table-list">
          <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px">
            <h5 style="margin: 0; font-size: 16px">表配置列表</h5>
            <button type="button" class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#addTableModal">+ 添加</button>
          </div>
          
          <!-- 搜索框 -->
          <div style="margin-bottom: 15px">
            <div style="position: relative">
              <input type="text" id="searchInput" class="form-control form-control-sm" placeholder="搜索表名或编码..." style="padding-right: 30px">
              <button type="button" id="clearSearch" class="btn btn-sm" style="position: absolute; right: 5px; top: 50%; transform: translateY(-50%); border: none; background: none; color: #6c757d; display: none" onclick="clearSearchInput()">×</button>
            </div>
          </div>

          <div id="tableListContainer">
            {% if tables %} {% for table in tables %}
            <div class="table-item {% if selected_table and table.id == selected_table.id %}active{% endif %}" onclick="location.href='?table_id={{ table.id }}'" data-search-text="{{ table.display_name|lower }} {{ table.table_code|lower }} {{ table.table_name|lower }}">
              <div class="table-display-name">{{ table.display_name }}</div>
              <div class="table-code">{{ table.table_code }} ({{ table.table_name }})</div>
            </div>
            {% endfor %} {% else %}
            <div style="text-align: center; padding: 30px 10px; color: #6c757d">暂无表配置</div>
            {% endif %}
          </div>
          
          <!-- 搜索无结果提示 -->
          <div id="noResults" style="text-align: center; padding: 30px 10px; color: #6c757d; display: none">未找到匹配的表配置</div>
        </div>

        <!-- 右侧：字段配置管理 -->
        <div class="config-panel">
          {% if selected_table %}
          <div class="panel-header">
            <div>
              <div class="panel-title">{{ selected_table.display_name }}</div>
              <small style="color: #6c757d">{{ selected_table.description }}</small>
            </div>
            <div class="btn-group-small">
              <button type="button" class="btn btn-outline-secondary btn-sm" data-bs-toggle="modal" data-bs-target="#editTableModal">编辑表配置</button>
              <form method="post" action="/configurable-config/table/toggle/" style="margin: 0;">
                {% csrf_token %}
                <input type="hidden" name="table_id" value="{{ selected_table.id }}" />
                <button type="submit" class="btn btn-outline-{% if selected_table.is_active %}warning{% else %}success{% endif %} btn-sm">
                  {% if selected_table.is_active %}禁用表{% else %}启用表{% endif %}
                </button>
              </form>
            </div>
          </div>

          <!-- 字段配置标签页 -->
          <ul class="nav nav-tabs" role="tablist">
            <li class="nav-item" role="presentation">
              <button class="nav-link active" id="update-tab" data-bs-toggle="tab" data-bs-target="#update-fields" type="button">修改字段配置</button>
            </li>
            <li class="nav-item" role="presentation">
              <button class="nav-link" id="query-tab" data-bs-toggle="tab" data-bs-target="#query-fields" type="button">查询字段配置</button>
            </li>
          </ul>

          <div class="tab-content">
            <!-- 修改字段配置 -->
            <div class="tab-pane fade show active" id="update-fields" role="tabpanel">
              <div style="margin-bottom: 15px">
                <button type="button" class="btn btn-success btn-sm" data-bs-toggle="modal" data-bs-target="#addFieldModal" onclick="setFieldType('update')">+ 添加修改字段</button>
              </div>
              <div class="table-responsive">
                <table class="table table-hover table-striped">
                  <thead>
                    <tr>
                      <th style="width: 11%">字段名</th>
                      <th style="width: 11%">显示名称</th>
                      <th style="width: 8%">数据类型</th>
                      <th style="width: 12%">下拉框分组</th>
                      <th style="width: 6%">必填</th>
                      <th style="width: 6%">可为空</th>
                      <th style="width: 12%">SQL文件名</th>
                      <th style="width: 7%">最大长度</th>
                      <th style="width: 5%">排序</th>
                      <th style="width: 7%">状态</th>
                      <th style="width: 15%">操作</th>
                    </tr>
                  </thead>
                  <tbody>
                    {% if update_fields %} {% for field in update_fields %}
                    <tr>
                      <td><code>{{ field.field_name }}</code></td>
                      <td>{{ field.display_name }}</td>
                      <td>{{ field.get_data_type_display }}</td>
                      <td>{% if field.dropdown_group %}{{ field.dropdown_group.group_name }}{% else %}-{% endif %}</td>
                      <td>{% if field.is_required %}是{% else %}否{% endif %}</td>
                      <td>{% if field.is_nullable %}是{% else %}否{% endif %}</td>
                      <td>{{ field.sql_file_name|default:"-" }}</td>
                      <td>{{ field.max_length|default:"-" }}</td>
                      <td>{{ field.sort_order }}</td>
                      <td><span class="badge badge-{% if field.is_active %}active{% else %}inactive{% endif %}">{% if field.is_active %}启用{% else %}禁用{% endif %}</span></td>
                      <td>
                        <div class="action-buttons">
                          <button type="button" class="btn btn-sm btn-outline-primary" onclick="editField({{ field.id }}, '{{ field.field_name }}', '{{ field.display_name }}', '{{ field.data_type }}', {{ field.is_required|lower }}, {{ field.is_nullable|lower }}, '{{ field.sql_file_name|default:'' }}', '{{ field.max_length|default:'' }}', '{{ field.default_value }}', {{ field.sort_order }}, 'update', '{{ field.dropdown_group.id|default:'' }}')">编辑</button>
                          <button type="button" class="btn btn-sm btn-outline-danger" onclick="deleteField({{ field.id }})">删除</button>
                          <form method="post" action="/configurable-config/field/toggle/" style="margin: 0;">
                            {% csrf_token %}
                            <input type="hidden" name="field_id" value="{{ field.id }}" />
                            <button type="submit" class="btn btn-sm btn-outline-{% if field.is_active %}warning{% else %}success{% endif %}">{% if field.is_active %}禁用{% else %}启用{% endif %}</button>
                          </form>
                        </div>
                      </td>
                    </tr>
                    {% endfor %} {% else %}
                    <tr>
                      <td colspan="11" class="text-center" style="padding: 40px">暂无修改字段配置</td>
                    </tr>
                    {% endif %}
                  </tbody>
                </table>
              </div>
            </div>

            <!-- 查询字段配置 -->
            <div class="tab-pane fade" id="query-fields" role="tabpanel">
              <div style="margin-bottom: 15px">
                <button type="button" class="btn btn-success btn-sm" data-bs-toggle="modal" data-bs-target="#addFieldModal" onclick="setFieldType('query')">+ 添加查询字段</button>
              </div>
              <div class="table-responsive">
                <table class="table table-hover table-striped">
                  <thead>
                    <tr>
                      <th style="width: 13%">字段名</th>
                      <th style="width: 13%">显示名称</th>
                      <th style="width: 10%">数据类型</th>
                      <th style="width: 13%">下拉框分组</th>
                      <th style="width: 8%">必填</th>
                      <th style="width: 10%">默认值</th>
                      <th style="width: 6%">排序</th>
                      <th style="width: 9%">状态</th>
                      <th style="width: 18%">操作</th>
                    </tr>
                  </thead>
                  <tbody>
                    {% if query_fields %} {% for field in query_fields %}
                    <tr>
                      <td><code>{{ field.field_name }}</code></td>
                      <td>{{ field.display_name }}</td>
                      <td>{{ field.get_data_type_display }}</td>
                      <td>{% if field.dropdown_group %}{{ field.dropdown_group.group_name }}{% else %}-{% endif %}</td>
                      <td>{% if field.is_required %}是{% else %}否{% endif %}</td>
                      <td>{{ field.default_value|default:"-" }}</td>
                      <td>{{ field.sort_order }}</td>
                      <td><span class="badge badge-{% if field.is_active %}active{% else %}inactive{% endif %}">{% if field.is_active %}启用{% else %}禁用{% endif %}</span></td>
                      <td>
                        <div class="action-buttons">
                          <button type="button" class="btn btn-sm btn-outline-primary" onclick="editField({{ field.id }}, '{{ field.field_name }}', '{{ field.display_name }}', '{{ field.data_type }}', {{ field.is_required|lower }}, false, '', '{{ field.max_length|default:'' }}', '{{ field.default_value }}', {{ field.sort_order }}, 'query', '{{ field.dropdown_group.id|default:'' }}')">编辑</button>
                          <button type="button" class="btn btn-sm btn-outline-danger" onclick="deleteField({{ field.id }})">删除</button>
                          <form method="post" action="/configurable-config/field/toggle/" style="margin: 0;">
                            {% csrf_token %}
                            <input type="hidden" name="field_id" value="{{ field.id }}" />
                            <button type="submit" class="btn btn-sm btn-outline-{% if field.is_active %}warning{% else %}success{% endif %}">{% if field.is_active %}禁用{% else %}启用{% endif %}</button>
                          </form>
                        </div>
                      </td>
                    </tr>
                    {% endfor %} {% else %}
                    <tr>
                      <td colspan="9" class="text-center" style="padding: 40px">暂无查询字段配置</td>
                    </tr>
                    {% endif %}
                  </tbody>
                </table>
              </div>
            </div>
          </div>
          {% else %}
          <div class="empty-state">
            <div>📋</div>
            <h5>请选择一个表配置</h5>
            <p>从左侧列表中选择一个表配置开始管理字段</p>
          </div>
          {% endif %}
        </div>
      </div>
    </div>

    <!-- 添加表配置模态框 -->
    <div class="modal fade" id="addTableModal" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <form method="post" action="/configurable-config/table/add/">
            {% csrf_token %}
            <div class="modal-header">
              <h5 class="modal-title">添加表配置</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
              <label for="table_name">数据库表名 *</label>
              <input type="text" class="form-control" id="table_name" name="table_name" required placeholder="例如: tphct01" />

              <label for="display_name">显示名称 *</label>
              <input type="text" class="form-control" id="display_name" name="display_name" required placeholder="例如: 供应商名称修改" />

              <label for="description">功能描述</label>
              <textarea class="form-control" id="description" name="description" rows="3"></textarea>
              
              <small class="text-muted" style="margin-top: 10px; display: block">注:表编码由系统自动生成,无需手动输入</small>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
              <button type="submit" class="btn btn-primary">添加</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- 编辑表配置模态框 -->
    {% if selected_table %}
    <div class="modal fade" id="editTableModal" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <form method="post" action="/configurable-config/table/edit/">
            {% csrf_token %}
            <input type="hidden" name="table_id" value="{{ selected_table.id }}" />
            <div class="modal-header">
              <h5 class="modal-title">编辑表配置</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
              <label>表编码</label>
              <input type="text" class="form-control" value="{{ selected_table.table_code }}" disabled />
              <small class="text-muted">表编码由系统自动生成,不可修改</small>

              <label for="edit_table_name">数据库表名 *</label>
              <input type="text" class="form-control" id="edit_table_name" name="table_name" value="{{ selected_table.table_name }}" required />

              <label for="edit_display_name">显示名称 *</label>
              <input type="text" class="form-control" id="edit_display_name" name="display_name" value="{{ selected_table.display_name }}" required />

              <label for="edit_description">功能描述</label>
              <textarea class="form-control" id="edit_description" name="description" rows="3">{{ selected_table.description }}</textarea>

              <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 15px; margin-bottom: 8px;">
                <label style="margin: 0;">启用的数据库配置</label>
                <button type="button" class="btn btn-sm btn-success" onclick="showAddDbConfigModal()">+ 快速添加数据库</button>
              </div>
              <div style="padding: 10px; border: 1px solid #dee2e6; border-radius: 4px; max-height: 200px; overflow-y: auto" id="dbConfigList">
                {% if all_db_configs %}
                  {% for db_config in all_db_configs %}
                  <div style="margin-bottom: 8px">
                    <input 
                      type="checkbox" 
                      id="db_{{ db_config.id }}" 
                      name="db_configs" 
                      value="{{ db_config.id }}"
                      {% if db_config in selected_table.database_configs.all %}checked{% endif %}
                    />
                    <label for="db_{{ db_config.id }}" style="margin: 0; font-weight: normal">
                      {{ db_config.config_name }} ({{ db_config.db_host }} - {{ db_config.db_name }})
                    </label>
                  </div>
                  {% endfor %}
                {% else %}
                  <small class="text-muted">暂无可用的数据库配置，点击上方按钮快速添加</small>
                {% endif %}
              </div>
              <small class="text-muted">选择该表生成SQL时需要包含的数据库</small>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
              <button type="submit" class="btn btn-primary">保存</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- 添加字段配置模态框 -->
    <div class="modal fade" id="addFieldModal" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <form method="post" action="/configurable-config/field/add/">
            {% csrf_token %}
            <input type="hidden" name="table_id" value="{{ selected_table.id }}" />
            <input type="hidden" name="field_type" id="add_field_type" value="update" />
            <div class="modal-header">
              <h5 class="modal-title">添加字段配置</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
              <label for="field_name">字段名 *</label>
              <input type="text" class="form-control" id="field_name" name="field_name" required placeholder="例如: SUPPLIER_NAME" />

              <label for="field_display_name">显示名称 *</label>
              <input type="text" class="form-control" id="field_display_name" name="display_name" required placeholder="例如: 供应商名称" />

              <label for="field_data_type">数据类型 *</label>
              <select class="form-select" id="field_data_type" name="data_type" required onchange="toggleDropdownGroup('field')">
                <option value="text">文本</option>
                <option value="number">数字</option>
                <option value="date">日期</option>
                <option value="dropdown">下拉框</option>
              </select>

              <div id="field_dropdown_group_container" style="margin-top: 10px; display: none;">
                <label for="field_dropdown_group">下拉框数据源分组 *</label>
                <select class="form-select" id="field_dropdown_group" name="dropdown_group">
                  <option value="">请选择下拉框数据源分组</option>
                  {% for group in dropdown_groups %}
                  <option value="{{ group.id }}">{{ group.group_name }} ({{ group.group_code }})</option>
                  {% endfor %}
                </select>
              </div>

              <div style="margin-top: 10px">
                <input type="checkbox" id="field_is_required" name="is_required" />
                <label for="field_is_required">必填字段</label>
              </div>

              <div style="margin-top: 10px">
                <input type="checkbox" id="field_is_nullable" name="is_nullable" checked />
                <label for="field_is_nullable">可为空（空值生成空字符串SQL）</label>
              </div>

              <div id="sql_file_name_group" style="margin-top: 10px">
                <label for="field_sql_file_name">SQL文件名</label>
                <input type="text" class="form-control" id="field_sql_file_name" name="sql_file_name" placeholder="例如：修改供应商名称" />
                <small class="text-muted">用于生成SQL文件名，多个字段用顿号连接</small>
              </div>

              <label for="field_max_length">最大长度</label>
              <input type="number" class="form-control" id="field_max_length" name="max_length" placeholder="仅文本字段需要" />

              <label for="field_default_value">默认值</label>
              <input type="text" class="form-control" id="field_default_value" name="default_value" placeholder="查询字段可设置默认值" />

              <label for="field_sort_order">排序顺序</label>
              <input type="number" class="form-control" id="field_sort_order" name="sort_order" value="0" />
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
              <button type="submit" class="btn btn-success">添加</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- 编辑字段配置模态框 -->
    <div class="modal fade" id="editFieldModal" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <form method="post" action="/configurable-config/field/edit/">
            {% csrf_token %}
            <input type="hidden" name="field_id" id="edit_field_id" />
            <div class="modal-header">
              <h5 class="modal-title">编辑字段配置</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
              <label for="edit_field_name">字段名 *</label>
              <input type="text" class="form-control" id="edit_field_name" name="field_name" required />

              <label for="edit_field_display_name">显示名称 *</label>
              <input type="text" class="form-control" id="edit_field_display_name" name="display_name" required />

              <label for="edit_field_data_type">数据类型 *</label>
              <select class="form-select" id="edit_field_data_type" name="data_type" required onchange="toggleDropdownGroup('edit_field')">
                <option value="text">文本</option>
                <option value="number">数字</option>
                <option value="date">日期</option>
                <option value="dropdown">下拉框</option>
              </select>

              <div id="edit_field_dropdown_group_container" style="margin-top: 10px; display: none;">
                <label for="edit_field_dropdown_group">下拉框数据源分组 *</label>
                <select class="form-select" id="edit_field_dropdown_group" name="dropdown_group">
                  <option value="">请选择下拉框数据源分组</option>
                  {% for group in dropdown_groups %}
                  <option value="{{ group.id }}">{{ group.group_name }} ({{ group.group_code }})</option>
                  {% endfor %}
                </select>
              </div>

              <div style="margin-top: 10px">
                <input type="checkbox" id="edit_field_is_required" name="is_required" />
                <label for="edit_field_is_required">必填字段</label>
              </div>

              <div style="margin-top: 10px">
                <input type="checkbox" id="edit_field_is_nullable" name="is_nullable" />
                <label for="edit_field_is_nullable">可为空（空值生成空字符串SQL）</label>
              </div>

              <div id="edit_sql_file_name_group" style="margin-top: 10px">
                <label for="edit_field_sql_file_name">SQL文件名</label>
                <input type="text" class="form-control" id="edit_field_sql_file_name" name="sql_file_name" placeholder="例如：修改供应商名称" />
                <small class="text-muted">用于生成SQL文件名</small>
              </div>

              <label for="edit_field_max_length">最大长度</label>
              <input type="number" class="form-control" id="edit_field_max_length" name="max_length" />

              <label for="edit_field_default_value">默认值</label>
              <input type="text" class="form-control" id="edit_field_default_value" name="default_value" />

              <label for="edit_field_sort_order">排序顺序</label>
              <input type="number" class="form-control" id="edit_field_sort_order" name="sort_order" />
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
              <button type="submit" class="btn btn-primary">保存</button>
            </div>
          </form>
        </div>
      </div>
    </div>
    {% endif %}

    <!-- 快速添加数据库配置模态框 -->
    <div class="modal fade" id="addDbConfigModal" tabindex="-1">
      <div class="modal-dialog">
        <div class="modal-content">
          <form id="addDbConfigForm">
            {% csrf_token %}
            <div class="modal-header">
              <h5 class="modal-title">快速添加数据库配置</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
              <label for="quick_config_name">配置名称 *</label>
              <input type="text" class="form-control" id="quick_config_name" name="config_name" required placeholder="例如: 生产环境数据库" />

              <label for="quick_db_type">数据库类型 *</label>
              <select class="form-select" id="quick_db_type" name="db_type" required>
                <option value="oracle">Oracle</option>
                <option value="mysql">MySQL</option>
                <option value="postgresql">PostgreSQL</option>
                <option value="sqlserver">SQL Server</option>
              </select>

              <label for="quick_db_host">数据库主机 *</label>
              <input type="text" class="form-control" id="quick_db_host" name="db_host" required placeholder="例如: 192.168.1.100" />

              <label for="quick_db_port">端口号 *</label>
              <input type="number" class="form-control" id="quick_db_port" name="db_port" required placeholder="例如: 1521" />

              <label for="quick_db_name">数据库名称 *</label>
              <input type="text" class="form-control" id="quick_db_name" name="db_name" required placeholder="例如: ORCL" />

              <label for="quick_db_user">用户名 *</label>
              <input type="text" class="form-control" id="quick_db_user" name="db_user" required placeholder="例如: admin" />

              <label for="quick_db_password">密码 *</label>
              <input type="password" class="form-control" id="quick_db_password" name="db_password" required />

              <div id="quickAddMessage" style="margin-top: 10px; display: none;"></div>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
              <button type="button" class="btn btn-primary" onclick="submitQuickDbConfig()">保存并选中</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      var currentFieldType = 'update';

      function setFieldType(type) {
        currentFieldType = type;
        document.getElementById('add_field_type').value = type;
        
        // 根据字段类型显示/隐藏SQL文件名配置
        var sqlFileNameGroup = document.getElementById('sql_file_name_group');
        if (type === 'update') {
          sqlFileNameGroup.style.display = 'block';
        } else {
          sqlFileNameGroup.style.display = 'none';
        }
      }
      
      // 切换下拉框分组显示/隐藏
      function toggleDropdownGroup(prefix) {
        var dataTypeSelect = document.getElementById(prefix + '_data_type');
        var dropdownGroupContainer = document.getElementById(prefix + '_dropdown_group_container');
        
        if (dataTypeSelect.value === 'dropdown') {
          dropdownGroupContainer.style.display = 'block';
        } else {
          dropdownGroupContainer.style.display = 'none';
        }
      }

      function editField(id, name, displayName, dataType, isRequired, isNullable, sqlFileName, maxLength, defaultValue, sortOrder, fieldType, dropdownGroupId) {
        document.getElementById('edit_field_id').value = id;
        document.getElementById('edit_field_name').value = name;
        document.getElementById('edit_field_display_name').value = displayName;
        document.getElementById('edit_field_data_type').value = dataType;
        document.getElementById('edit_field_is_required').checked = isRequired;
        document.getElementById('edit_field_is_nullable').checked = isNullable;
        document.getElementById('edit_field_sql_file_name').value = sqlFileName;
        document.getElementById('edit_field_max_length').value = maxLength;
        document.getElementById('edit_field_default_value').value = defaultValue;
        document.getElementById('edit_field_sort_order').value = sortOrder;
        
        // 设置下拉框分组
        if (dropdownGroupId) {
          document.getElementById('edit_field_dropdown_group').value = dropdownGroupId;
        } else {
          document.getElementById('edit_field_dropdown_group').value = '';
        }
        
        // 根据数据类型显示/隐藏下拉框分组
        toggleDropdownGroup('edit_field');
        
        // 根据字段类型显示/隐藏SQL文件名配置
        var editSqlFileNameGroup = document.getElementById('edit_sql_file_name_group');
        if (fieldType === 'update') {
          editSqlFileNameGroup.style.display = 'block';
        } else {
          editSqlFileNameGroup.style.display = 'none';
        }

        var modal = new bootstrap.Modal(document.getElementById('editFieldModal'));
        modal.show();
      }

      function deleteField(id) {
        if (confirm('确定要删除这个字段配置吗？')) {
          var form = document.createElement('form');
          form.method = 'POST';
          form.action = '/configurable-config/field/delete/';

          var csrfInput = document.createElement('input');
          csrfInput.type = 'hidden';
          csrfInput.name = 'csrfmiddlewaretoken';
          csrfInput.value = '{{ csrf_token }}';

          var fieldInput = document.createElement('input');
          fieldInput.type = 'hidden';
          fieldInput.name = 'field_id';
          fieldInput.value = id;

          form.appendChild(csrfInput);
          form.appendChild(fieldInput);
          document.body.appendChild(form);
          form.submit();
        }
      }

      // 消息自动消失
      (function () {
        var success = document.getElementById('saveSuccess');
        var err = document.getElementById('saveError');

        function fadeOut(el) {
          if (el) {
            setTimeout(function () {
              el.style.opacity = '0';
              setTimeout(function () {
                if (el && el.parentNode) el.parentNode.removeChild(el);
              }, 300);
            }, 2500);
          }
        }

        fadeOut(success);
        fadeOut(err);
      })();
      
      // 显示快速添加数据库配置模态框
      function showAddDbConfigModal() {
        var modal = new bootstrap.Modal(document.getElementById('addDbConfigModal'));
        modal.show();
      }

      // 提交快速添加数据库配置
      function submitQuickDbConfig() {
        var form = document.getElementById('addDbConfigForm');
        var formData = new FormData(form);
        var messageDiv = document.getElementById('quickAddMessage');

        // 显示加载状态
        messageDiv.style.display = 'block';
        messageDiv.className = 'alert alert-info';
        messageDiv.textContent = '正在添加数据库配置...';

        fetch('/database-config/add/', {
          method: 'POST',
          body: formData,
          headers: {
            'X-Requested-With': 'XMLHttpRequest'
          }
        })
        .then(response => response.json())
        .then(data => {
          if (data.success) {
            messageDiv.className = 'alert alert-success';
            messageDiv.textContent = '数据库配置添加成功！';
            
            // 添加新的数据库配置到列表
            var dbConfigList = document.getElementById('dbConfigList');
            var newCheckbox = document.createElement('div');
            newCheckbox.style.marginBottom = '8px';
            newCheckbox.innerHTML = `
              <input 
                type="checkbox" 
                id="db_${data.db_config_id}" 
                name="db_configs" 
                value="${data.db_config_id}"
                checked
              />
              <label for="db_${data.db_config_id}" style="margin: 0; font-weight: normal">
                ${data.config_name} (${data.db_host} - ${data.db_name})
              </label>
            `;
            
            // 如果列表为空，先清空提示文字
            if (dbConfigList.querySelector('.text-muted')) {
              dbConfigList.innerHTML = '';
            }
            
            dbConfigList.appendChild(newCheckbox);
            
            // 1秒后关闭模态框
            setTimeout(function() {
              var modal = bootstrap.Modal.getInstance(document.getElementById('addDbConfigModal'));
              modal.hide();
              form.reset();
              messageDiv.style.display = 'none';
            }, 1000);
          } else {
            messageDiv.className = 'alert alert-danger';
            messageDiv.textContent = '添加失败: ' + (data.error || '未知错误');
          }
        })
        .catch(error => {
          messageDiv.className = 'alert alert-danger';
          messageDiv.textContent = '添加失败: ' + error.message;
        });
      }

      // 搜索功能
      (function() {
        var searchInput = document.getElementById('searchInput');
        var clearBtn = document.getElementById('clearSearch');
        var tableItems = document.querySelectorAll('.table-item');
        var noResults = document.getElementById('noResults');
        var debounceTimer;
        
        if (!searchInput) return;
        
        // 防抖搜索
        searchInput.addEventListener('input', function() {
          clearTimeout(debounceTimer);
          debounceTimer = setTimeout(function() {
            filterTables();
          }, 300);
          
          // 显示/隐藏清除按钮
          if (searchInput.value) {
            clearBtn.style.display = 'block';
          } else {
            clearBtn.style.display = 'none';
          }
        });
        
        // 清除搜索
        window.clearSearchInput = function() {
          searchInput.value = '';
          clearBtn.style.display = 'none';
          filterTables();
          searchInput.focus();
        };
        
        // 过滤表配置
        function filterTables() {
          var keyword = searchInput.value.toLowerCase().trim();
          var visibleCount = 0;
          
          tableItems.forEach(function(item) {
            var searchText = item.getAttribute('data-search-text') || '';
            if (!keyword || searchText.indexOf(keyword) !== -1) {
              item.style.display = '';
              visibleCount++;
            } else {
              item.style.display = 'none';
            }
          });
          
          // 显示/隐藏无结果提示
          if (keyword && visibleCount === 0) {
            noResults.style.display = 'block';
          } else {
            noResults.style.display = 'none';
          }
        }
      })();
    </script>
  </body>
</html>
