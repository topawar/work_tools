# 增强的原单位名称校验方案

## 问题背景
原有校验方案存在问题：当数据库中存在多个同名组织机构（不同编码）时，用户从自动完成中选择了带编码的选项（如"中核（上海）供应链管理有限公司西北分公司-99280563919161110148"），但校验时只用名称查询，无法精确匹配到用户选择的那条记录。

## 解决方案

### 1. 统一的显示格式
- **自动完成显示**：`公司名称-编码`
- **用户选择**：`中核（上海）供应链管理有限公司西北分公司-99280563919161110148`
- **手动输入**：支持纯名称或名称-编码格式

### 2. 智能解析逻辑
```python
def parse_unit_name_with_code(input_value):
    """
    解析单位名称输入，支持两种格式：
    1. 纯名称：中核（上海）供应链管理有限公司
    2. 名称-编码：中核（上海）供应链管理有限公司-99280563919161110148
    """
```

**解析规则**：
- 如果包含 `-` 且最后部分像编码（长度>5且包含数字），则分离名称和编码
- 否则视为纯名称

### 3. 分层校验策略

#### 情况1：输入包含编码
```
输入：中核（上海）供应链管理有限公司西北分公司-99280563919161110148
校验：WHERE company_name='中核（上海）供应链管理有限公司西北分公司' AND company_code='99280563919161110148'
结果：精确匹配，校验通过 ✓
```

#### 情况2：输入只有名称，且名称唯一
```
输入：中核新疆供应链有限公司
校验：WHERE company_name='中核新疆供应链有限公司'
结果：找到1条记录，校验通过 ✓
```

#### 情况3：输入只有名称，但存在多条记录
```
输入：中核（上海）供应链管理有限公司西北分公司
校验：WHERE company_name='中核（上海）供应链管理有限公司西北分公司'
结果：找到2条记录，提示选择具体编码 ⚠
错误信息：原起草单位名称"中核（上海）供应链管理有限公司西北分公司"存在多条记录，请选择具体编码：99280563919161110148、1912178770857907780
```

#### 情况4：名称不存在
```
输入：不存在的公司名称
校验：WHERE company_name='不存在的公司名称'
结果：找到0条记录，校验失败 ✗
错误信息：原起草单位名称"不存在的公司名称"在数据库中不存在
```

#### 情况5：编码错误
```
输入：中核（上海）供应链管理有限公司西北分公司-错误编码
校验：WHERE company_name='...' AND company_code='错误编码'
结果：找到0条记录，校验失败 ✗
错误信息：原起草单位名称"中核（上海）供应链管理有限公司西北分公司"（编码：错误编码）在数据库中不存在
```

## 用户体验优化

### 1. 自动完成功能
- 搜索API已返回 `公司名称-编码` 格式
- 用户输入时看到：
  ```
  中核（上海）供应链管理有限公司西北分公司-99280563919161110148
  中核（上海）供应链管理有限公司西北分公司-1912178770857907780
  ```

### 2. 错误提示优化
- **精确错误**：明确指出是名称不存在还是编码不存在
- **建设性提示**：当存在多条记录时，列出所有可用编码
- **友好格式**：使用中文标点和清晰的表述

### 3. 输入灵活性
- **支持自动完成选择**：用户点击选择带编码的选项
- **支持手动输入**：用户可以手动输入名称或名称-编码
- **容错处理**：自动去除首尾空格，处理空输入

## 技术实现

### 1. 核心函数
```python
def parse_unit_name_with_code(input_value):
    # 解析输入，返回 (name, code)

def validate_original_unit_names(old_drafting_name=None, old_party_name=None):
    # 校验原单位名称，返回错误列表
```

### 2. 集成点
- **单条记录模式**：表单提交时校验
- **Excel批量导入**：逐行校验，显示行号

### 3. 数据库查询优化
- **精确查询**：使用名称+编码的复合条件
- **性能考虑**：利用现有索引，避免全表扫描
- **结果处理**：区分0条、1条、多条记录的不同情况

## 测试用例

### 正常情况
| 输入 | 预期结果 |
|------|----------|
| `中核新疆供应链有限公司` | ✓ 校验通过（唯一记录） |
| `中核（上海）供应链管理有限公司西北分公司-99280563919161110148` | ✓ 校验通过（精确匹配） |

### 异常情况
| 输入 | 预期结果 |
|------|----------|
| `不存在的公司` | ✗ 名称不存在 |
| `中核（上海）供应链管理有限公司西北分公司-错误编码` | ✗ 编码不存在 |
| `中核（上海）供应链管理有限公司西北分公司` | ⚠ 提示选择编码 |

### 边界情况
| 输入 | 预期结果 |
|------|----------|
| 空字符串 | 跳过校验 |
| 只有空格 | 跳过校验 |
| `公司名称-短编码` | 视为纯名称 |

## 优势

1. **精确匹配**：解决了同名组织机构的歧义问题
2. **用户友好**：提供清晰的错误提示和建议
3. **向后兼容**：支持原有的纯名称输入方式
4. **性能优良**：使用高效的数据库查询
5. **扩展性好**：易于添加新的校验规则

## 使用建议

1. **优先使用自动完成**：避免手动输入错误
2. **选择带编码的选项**：确保精确匹配
3. **注意错误提示**：根据提示选择正确的编码
4. **保持数据更新**：确保组织机构数据库是最新的

## 后续优化

1. **模糊匹配建议**：当精确匹配失败时，提供相似名称建议
2. **编码格式验证**：增强编码格式的识别准确性
3. **批量处理优化**：对Excel导入进行性能优化
4. **用户行为分析**：统计常见错误，优化用户体验